---
title: List teams
description: Retrieve all Formula 1 teams with their drivers and calculated points
---

## Endpoint

```
GET /api/teams
```

Retrieves all Formula 1 teams with their associated drivers and calculated total points. The total points are computed by summing all race result points for each driver in the team.

## Request

This endpoint requires no parameters or authentication.

## Response

Returns an object containing an array of teams with their drivers and calculated points.

<ResponseField name="data" type="array" required>
  Array of team objects with calculated points
  
  <Expandable title="Team object">
    <ResponseField name="teamId" type="string" required>
      Unique identifier for the team
    </ResponseField>
    
    <ResponseField name="name" type="string">
      Full name of the team
    </ResponseField>
    
    <ResponseField name="nationality" type="string">
      Team's nationality
    </ResponseField>
    
    <ResponseField name="url" type="string">
      URL to the team's official page
    </ResponseField>
    
    <ResponseField name="teamLogo" type="string">
      URL to the team's logo image
    </ResponseField>
    
    <ResponseField name="totalPoints" type="number" required>
      Total championship points accumulated by all team drivers. Calculated by summing all race result points for each driver in the team.
    </ResponseField>
    
    <ResponseField name="Drivers" type="array" required>
      Array of drivers associated with this team
      
      <Expandable title="Driver object">
        <ResponseField name="driverId" type="string" required>
          Unique identifier for the driver
        </ResponseField>
        
        <ResponseField name="givenName" type="string">
          Driver's first name
        </ResponseField>
        
        <ResponseField name="familyName" type="string">
          Driver's last name
        </ResponseField>
        
        <ResponseField name="driverImage" type="string">
          URL to the driver's image
        </ResponseField>
        
        <ResponseField name="Results" type="array">
          Array of race results for this driver
          
          <Expandable title="Result object">
            <ResponseField name="points" type="number" required>
              Points earned in a specific race
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Points calculation logic

The `totalPoints` field is calculated using the following logic from `/home/daytona/workspace/source/server/api/teams/index.ts:25-38`:

```typescript
const totalPoints = team.Drivers.reduce((sum, driver) => {
    return (
        sum +
        driver.Results.reduce((driverPoints, result) => {
            return driverPoints + result.points;
        }, 0)
    );
}, 0);
```

This sums all race result points for every driver in the team to get the team's total championship points.

## Example

<CodeGroup>
```bash cURL
curl https://your-domain.com/api/teams
```

```javascript JavaScript
const response = await fetch('https://your-domain.com/api/teams');
const data = await response.json();
```
</CodeGroup>

### Response

```json
{
  "data": [
    {
      "teamId": "red_bull",
      "name": "Red Bull Racing",
      "nationality": "Austrian",
      "url": "https://www.redbullracing.com",
      "teamLogo": "https://example.com/logos/red_bull.png",
      "totalPoints": 860,
      "Drivers": [
        {
          "driverId": "max_verstappen",
          "givenName": "Max",
          "familyName": "Verstappen",
          "driverImage": "https://example.com/drivers/verstappen.jpg",
          "Results": [
            { "points": 25 },
            { "points": 18 },
            { "points": 25 }
          ]
        },
        {
          "driverId": "sergio_perez",
          "givenName": "Sergio",
          "familyName": "Pérez",
          "driverImage": "https://example.com/drivers/perez.jpg",
          "Results": [
            { "points": 15 },
            { "points": 12 },
            { "points": 18 }
          ]
        }
      ]
    }
  ]
}
```